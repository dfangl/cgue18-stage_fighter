window = {
    width=1280,
    height=768,
    vsync=true,

    -- -1 == false, 0 == primary
    fullscreen=-1,

    -- -1 == Monitor Default
    refreshRate=-1,
    gamma=1.0
}

camera = {
    fov=45
}

debug = {
    bullet=false,
    opengl=true
}