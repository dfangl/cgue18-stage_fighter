window = {
    width=1280,
    height=1024,
    vsync=true,
    fullscreen=false,

    -- 0 == Default Monitor
    refreshRate=0
}

camera = {
    fov=45
}

debug = {
    bullet=true,
    opengl=true
}